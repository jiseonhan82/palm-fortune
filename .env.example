# 커플 궁합 기능용 Supabase 설정 (supabase/schema.sql 실행 후 콘솔 → Project Settings → API 에서 확인)
# 이 값들은 공개돼도 안전하도록 설계된 값입니다(RLS로 접근 제어) — 그래도 저장소엔 직접 커밋하지 않고
# 이 파일을 복사해 .env로 만들어 쓰세요: cp .env.example .env
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
